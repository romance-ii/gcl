#line 40 "configure"
#include "confdefs.h"
#include <stdio.h>
main()
{
  char *b = (void *) malloc(1000);
  FILE *fp = fopen("conftest1","w");
  fprintf(fp,"0x%x",((unsigned int) b) & ~0xffffff);
  fclose(fp);
}
